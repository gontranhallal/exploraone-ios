name: Build Unsigned iOS IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up Xcode Version
        run: sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Install Tuist via Homebrew
        env:
          HOMEBREW_NO_REQUIRE_TAP_TRUST: "1"
        run: |
          brew install tuist

      - name: Resolve Dependencies with Tuist
        env:
          HOMEBREW_NO_REQUIRE_TAP_TRUST: "1"
        run: |
          # Usamos 'tuist install' para descargar las dependencias vinculadas a tu Workspace.swift
          tuist install

      - name: Find Workspace and Build
        run: |
          # Buscamos el archivo de espacio de trabajo (.xcworkspace o .swiftworkspace)
          WORKSPACE=$(find . -name "*.xcworkspace" -o -name "*.swiftworkspace" | head -n 1)
          echo "Workspace localizado en ruta: $WORKSPACE"
          
          if [ -z "$WORKSPACE" ]; then
            echo "Error critico: No se localizo el espacio de trabajo."
            exit 1
          fi
          
          # Forzamos un esquema base compatible con los empaquetadores web de Median
          SCHEME="App"
          echo "Usando el esquema de compilacion: $SCHEME"
          
          xcodebuild -workspace "$WORKSPACE" \
                     -scheme "$SCHEME" \
                     -configuration Release \
                     -archivePath $HOME/build/App.xcarchive \
                     clean archive \
                     CODE_SIGNING_ALLOWED=NO

      - name: Export Unsigned IPA
        run: |
          mkdir -p payload/Payload
          cp -r $HOME/build/App.xcarchive/Products/Applications/*.app payload/Payload/
          cd payload
          zip -r ../App-Unsigned.ipa Payload
          
      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: App-Unsigned-IPA
          path: App-Unsigned.ipa
